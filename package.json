{
  "name": "agent-studio-ai",
  "version": "1.0.0",
  "description": "AI Agents Marketplace Frontend",
  "scripts": {
    "start": "serve .",
    "dev": "live-server --port=3000",
    "build": "mkdir -p dist && cp -r index.html about.html ai-agents.html contact.html for-developers.html for-users.html login.html pricing.html signup.html css js images dist/",
    "postbuild": "node build-validation.js",
    "security-check": "npm audit"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}